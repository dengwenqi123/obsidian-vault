```
1. CT用例稳定性测试工具
CT 稳定性执行pipeline 自动执行流程研发完成
实现用例失败自动化创建JIRA指派给相应研发
上线了 sim-vela/goldfish-v7a/qemu-miwear CT 用例稳定性测试(每个产品4个并发循环测试)。

1. VTF测试框架上线
VTF测试框架已合入班车分支，sim-vela/v7a-ap/qemu-mwear/sim-miwear daily任务已稳定，周四完成珊瑚海任务切换，11个CT任务已测试稳定，语句周四完成切换。

列大纲，做规划


在页面的顶部居中增加 tab切换，包含两个tab， 单产品用例、总体用例，tab显示需要美观，操作简洁；当前index.vue中的内容作为一第一个tab显示内容，新建一个总体用例(summary_case.vue)的组件，点击总体用例tab切换加载summary_case.vue组件中的数据；在summary_case.vue中需要包含测试用例占比、团队总代码覆盖率(参考index.vue中的团队代码覆盖率)、团队总体代码覆盖率趋势(参考index.vue中的团队代码变化趋势);在底部增加团队总体BUG列表；请不要修改index.vue中已有的的逻辑和交互； 
```

```
团队图表
- index.vue 中tab(单产品用例/总用例) 顶部居中，并且按钮美观
- 总用例分布ref="caseDistributionChart" 与index.vue文件中 ref="testcasePie"功能、数据和交互完全一样，查询数据的接口也一样，请将index.vue 中testcasePie 使用的功能也复制到SummaryCase.vue中
- 团队代码覆盖率趋势ref="coverageTotalTrendChart" ref="gcov_testcase_trend"功能、数据和交互完全一样,查询数据的接口也一样，请将index.vue 中testcasePie 使用的功能也复制到SummaryCase.vue中
---
ref="gcov_total_testcase_trend" 和index.vue中ref="gcov_testcase_trend" 功能完全一致，
- chartGcovTotalLineType 对应 chartGcovLineType
- refreshGcovTotalLineChart 对应 refreshGcovLineChart
- toggleGcovTotalLineFullScreen 对应 toggleGcovLineFullScreen
- gcovLineTotalTableData 对应 gcovLineTableData
请完成gcov_total_testcase_trend 中的功能，参考index.vue中的gcov_testcase_trend
```

```
server 端实现
1. 查询trunk分支 所有用例的distinct 值
2. 查询
```
飞书文档导出， 可以先导出为一份markdown格式的文档，然后将markdown 文档转换为飞书文档，附加链接到通知中。
- 比如汇总报告
- 性能报告


CT/Daily 修改为弹窗形式，弹窗中显示 CT任务链接，以及每一次构建记录和对应的镜像包，镜像包可能有多个，需要以树形图表展示，同时如果可以需要标记本次CT执行的结果和CI任务的结果。

```
1. monkey自动化
2. vtf 任务切换
3. 数据看板
4. 代码覆盖度数据同步
5. diagnose tool的数据统计看板
6. 奖项任务拆分与规划
7. 自动化测试任务执行时间小于10min,
8. 用例数据的分组
```
## 上线更新内容
```
ALTER TABLE vela_testcase_result ADD INDEX idx_branch (branch);
alter table vela_coralsea_bug_summary MODIFY  
COLUMN labels varchar(356) default '' comment 'jira标签'

alter table test_issues 
ADD COLUMN assignee varchar(64) default '' comment '指派人';
ADD COLUMN priority varchar(32) default '' comment '优先级';

```
需要增加代码测试覆盖度汇总表，记录汇总信息， 包括 id, 平均代码覆盖度，ci_task_id,测试覆盖度链接、product、 branch ,日期
需要针对代码覆盖度数据没有的情况，增加飞书提醒
```sql
CREATE TABLE `code_coverage_summary` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
  `average_coverage` DECIMAL(5,2) NOT NULL COMMENT '平均代码覆盖度（单位：%）',
  `ci_task_id` VARCHAR(256) NOT NULL COMMENT 'CI任务ID',
  `task_key` VARCHAR(128) NOT NULL COMMENT 'task_key',
  `coverage_report_url` VARCHAR(255) NOT NULL COMMENT '测试覆盖度报告链接',
  `product` VARCHAR(50) NOT NULL COMMENT '产品名称',
  `branch` VARCHAR(128) NOT NULL COMMENT '代码分支',
  `report_date` DATE NOT NULL COMMENT '报告日期',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '记录创建时间',
  
  PRIMARY KEY (`id`),
  UNIQUE KEY `uniq_ci_task` (`ci_task_id`), -- 假设每个CI任务ID唯一
  INDEX `idx_product` (`product`),
  INDEX `idx_branch` (`branch`),
  INDEX `idx_date` (`report_date`)
) ENGINE=InnoDB COMMENT='代码覆盖度汇总表';
```
1. 用例总数需要重新计算，针对新的VTF框架

需要增加总用例记录表， 记录不同产品(daily)任务每天的测试用例总数，包括 id、ci_task_id、product、branch、日期
```sql
CREATE TABLE `test_case_summary` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '自增主键',
  `ci_task_id` VARCHAR(256) NOT NULL COMMENT '关联的CI任务ID',
  `task_key` VARCHAR(128) NOT NULL COMMENT 'task_key',
  `product` VARCHAR(50) NOT NULL COMMENT '产品名称',
  `branch` VARCHAR(128) NOT NULL COMMENT '代码分支',
  `report_date` DATE NOT NULL COMMENT '统计日期',
  `test_case_total` INT UNSIGNED NOT NULL COMMENT '总测试用例数',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '记录创建时间',

  PRIMARY KEY (`id`),
  UNIQUE KEY `uniq_ci_task` (`ci_task_id`), -- 每个CI任务唯一
  INDEX `idx_product_branch` (`product`, `branch`), -- 联合查询优化
  INDEX `idx_date` (`report_date`)
) ENGINE=InnoDB COMMENT='测试用例总数日报表';

  


CREATE INDEX idx_branch ON vela_testcase_result(branch);
CREATE INDEX idx_method_name ON vela_testcase_result(method_name);

alter table vela_autotest_gcov  
    add domain varchar(64) null after team;
```
每天提报的自动化测试JIRA 总个数， 按照创建日期，计算每天提报jira的个数

## 看板优化
```
在 `@index.vue` 和 `@SummaryCase.vue` 这两个文件里，存在多个以 `FullScreen` 结尾的函数，例如 `toggleGcovTotalLineFullScreen`、`toggleBugFullScreen` 等。为了提升代码的可维护性和复用性，需要将这些函数抽取出来，封装成一个公共函数。要求抽取后的公共函数能保持原函数的功能不变。请完成此代码抽取和封装工作。
```

```
周报
x4b 、o62百度地图SDK导出
1. 完成自动化测试看板(团队指标看板) 功能研发，预计本周会上线。
2. VTF测试任务完成9个daily任务， 5个CT任务上线，预计本周会完成全部sim/qemu任务切换上线。
3. CT稳定性测试工具完成自动创建JIRA优化，工具发现的问题会自动给研发指派JIRA

4. 完成自动化测试看板（即团队指标看板）的功能研发工作，计划于本周正式上线。

5. 在VTF测试任务执行中，已完成9项 daily 任务，5项CT任务也已成功上线。按照当前进度，预计本周内能够完成全部 sim/qemu 任务的切换与上线工作。

6. CT 稳定性测试工具已完成自动创建 JIRA 功能的优化。如今，测试工具发现问题，将会自动为研发人员指派JIRA单。
```