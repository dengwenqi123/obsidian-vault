工作智能体
```
1. 上传的附件存储在什么地方
   
2. AI 在分析的时候能方便的拿到这些附件信息吗
```
1.  ai 调用这些skills, 能直接分析完成问题吗，分析过程中的中间过程和最终结论能在平台上显示出来
2. 能自定义agent 和 skills
3. 需要能支持定义解Bug 的工作流程
4. 可以给agent 默认设置工作路径； 如果需要修改bug, 需要使用git-worktree 进行操作


# 自动化
自动化做成dclaw 一样，


ai 在分析完成后，自动进行指派
# bugfix
https://jira.example.com/browse/VELAPLATFO-89319 给的jira 链接不正确，
## 部署
1. 我需要将其部署到matrix 节点上。
2. 需要接入cas 进行登陆
3. 需要新建一个仓库，存储这些代码

将 vela08-vela11 注册到mutc,并且在不同的机器设置不同的源码。 vela08 vela09 用于穿戴主线。
需要将molicat 部署到matrix 上，同时需要增加cas 验证。
用户通过cas 登陆后，默认都是普通研发
需要申请 progresssql 进行数据存储

需要在任务 中新增 飞书通知(任务完成后发送结果到飞书私聊)

任务支持定时触发 和手动触发，
需要定义agent, 在agent 中定义完善的分析流程。
crash 问题使用 crash-analysys gdb-start 进行分析
需要申请域名，数据库