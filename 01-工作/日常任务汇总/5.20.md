1. 将jira bug 导入到multica 平台
2. 将 coverity 数据导入到数据库， 基于jira 去查询
	```
	message:VELAFORCAR-14087
	```
	trunk-26rc01 的工具下载有问题， 建议采用 分支转换
	需要在 11点检查自动提单的流程
	汽车项目的提单重构
	
mify 账号如何给大家申请更多的额度，能统计到他实际使用了多少
需要确认是否已经包含了基于日志分析的问题单dup
1. AI 可以基于之前的问题继续提问
AI 分析完成后，没有自动将消息pin 到顶部
1.将所有的
将聊天弹窗，在左侧工作区有一个入口，点击可以直接以一个新的页面显示聊天框

2.  关键字就是 JIRA 号
3. 同步需要和jira 一样，首先显示description 信息

需要将所有的Multica 替换为 VelaAgent

1. 需要在agent 中能直接分析