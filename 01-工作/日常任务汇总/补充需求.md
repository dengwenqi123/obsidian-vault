1. 可以在管理系统中，在线调试该问题， 通过websocket 或者 http 形式 链接， 类似vcb
2.  研发也可以在后台管理系统中 对自己的用例设置压测，AI 自动复现问题。

3. 点击 构建，能跳转到构建任务页面
4. 分析结论需要单独写到一个文件中，并且上传

需要根据 不同的 类型，创建不同的提示词
有一个 "exception_type": "busy_loop", 字段进行区分
将所有的信息都补充 访问页面中，
1. 之前的任务链接
2. 在飞书卡片中需要有当前的详情页

点击执行机， 可以快速链接到之前运行的matrix 容器中
在 ai agent 启动服务

可以在管理页面中，点击聊天窗口，连接到 vtf 中的 ai agent session 中，发送消息，在vtf 中的 ai agent 能收到这些消息继续进行在线调试。

总结常用场景，压测
如果說是 test_os , 壓測次數需要小於10次

# 上线准备
## matrix 节点
1. 将 claude 的上下文设置为 1M
2. 需要配置 mcp 服务
3. 需要安装 pynuttx 下的依赖库
4. pip3 install galaxy-fds-sdk
5. 更新 ai_agent 流程

## VTF 
1. 设置传递 open_patches
2. 需要传递 branch
3. 需要上传 session_log 和 report_url
4. 第二次busyloop 没有上报数据
5. 需要有配置，能配置压测任务一旦有失败应该快速的终止执行

## ai_agent
1. 需要生成 report_url 报告
2. 需要更新提示词

## 云端平台
1. 将失败用例 压测次数设置为 2次。 一旦失败，需要快速的终止执行后续的用例 已完成： --stop-after-first-failure
2. 更新matrix 节点的链接
3. workflow 节点

## 系统组
1. 需要补充不同场景的调试环境， 用于调整和优化提示词
2.  创建一个系统组的群，在群里发送通知， 拉 几个主要负责调试的同学
3. 需要将之前的完整信息补充完成，之前的构建镜像包、构建日志 自动化测试任务
4. 将open_patches 放到上面 done
5. 如果 ai 不能直接解决，可以让ai 举手
6.  需要一个额外的表，记录所有之前测试的详细信息
	1. 构建镜像包
	2. 构建日志
	3. 自动化测试任务链接

全流程再测试一遍，没问题部署到线上运行
总结之前的patch， 通过这种方式进行压测和验证
三种类型 crash busyloop testcase

将验证轮数修改为 2轮


上线， 需要开启配置， --ai 开启ai 分析
需要开通gdb,  需要传递 gdb_port
https://coralsea.miot.mioffice.cn/project/vela/tasks/1239/daghistory/Vela%20VTF%20goldfish-armeabi-v7a-ap%20dev-system%20daily%20test

如果是同一个patch， 如何

明天早上拉 yuanchunwei 对齐耳机项目的B/C 类问题。
1. 提报给是哪个模块的研发
2. 完成的AI 思考和分析过程存储， 一旦研发反馈有用，则会自动总结为标准的skills
3. 