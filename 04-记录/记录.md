1. 记录每个patch 过CI的时间，即所有自动化测试中耗时最长的时间，需要记录到一个表里。每天计算昨天的
```
patch_info, cost_time, record_time,branch
CREATE TABLE vela_ct_cost_records (
    id INT AUTO_INCREMENT PRIMARY KEY,  -- 自增主键
    patch_info VARCHAR(1024) NOT NULL,  -- 补丁信息，最大长度为 1024 的字符串
    cost_time DECIMAL(6,2) NOT NULL,    -- 耗时，DECIMAL(6,2) 表示最多 6 位数字，其中 2 位小数
    record_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,  -- 记录时间，默认为当前时间
    branch VARCHAR(64) NOT NULL        -- 分支名称
);
```

```css
**`li p { display: inline; }`**

li {
    list-style-position: inside;
    white-space: nowrap; /* 防止文本换行 */
}
```

```
测试看板
项目数、项目覆盖率
crash bug数 crash bug覆盖率
所有内部crash占有率， 内部crash 数据通过婷婷提供的看板

二级指标
使用人数、 crash包含dump文件

```


```
需要实现一个 调试指标看板
未贴链接有一个buglist

```
# Q1 自动化测试复盘
## 虚拟设备池搭建
基于matrix平台(云基础设施)，实现trunk分支所有虚拟设备(SIM/Qemu)在云端实现动态可伸缩虚拟设备池搭建。
完成了Vela 虚拟设备测试资源的规范与资源划分
执行效率的提升50%以上

1. **全量虚拟设备云端化升级**
    
    - 基于Matrix云平台完成trunk分支全量虚拟设备（SIM/Qemu）的容器化改造
        
    - 建成弹性伸缩虚拟设备池，支持秒级资源调度（扩容/缩容响应时间≤3s）
        
2. **测试资源标准化管理**
    
    - 制定《Vela虚拟设备资源管理规范》，建立资源分级标准（S/A/B三类）
        
    - 实现测试环境资源隔离，冲突率降低至0.5%以下
        
3. **效能突破性提升**
    
    - 测试任务平均执行效率提升52%（原基准周期4.2h→2.0h）
        
    - 单日最大并发测试容量提升至300+设备实例
## VTF框架上线
VTF框架合入班车分支，完成 班车分支 CT 15个产品、Daily 12个产品测试框架切换。
完成trunk分支全部用例迁移，
切换之后15个CT任务稳定运行，未出现偶发失败问题，daily任务9个产品用例全部成功，3个产品有用例失败，失败用例都已覆盖JIRA跟踪

1. 成功将VTF测试框架合入班车分支，完成所有Sim/Qemu(15个CT产品及12个Daily产品)的测试框架切换至VTF框架
2. 实现trunk分支全量用例（100%）切换至VTF框架运行
3. 框架切换后用例运行稳定性：
    - CT任务：15个产品保持100%通过率，未出现偶发性失败
    - Daily任务：9个产品Daily全用例执行成功，3个产品出现用例失败(非框架问题)




## 自动化测试数据看板
1. 增加代码测试覆盖度，按团队、领域分类柱状图展示
2. 测试看板增加了产品测试和总体测试用例
3. 自动化测试看板作为团队用例关注统一的指标看板

```
总共交付了几个看板，覆盖了xx xx 的
交付了3项自动化测试看板，包括代码测试覆盖度、CT执行耗时分析、团队总体数据看板

select * from vela_coralsea_bug_summary; -- task_key 为 diagnostic_tool
labels 256
[VELAPLATFO-49029](https://jira.n.xiaomi.com/browse/VELAPLATFO-49029)

```

```
周报内容
dignose调试工具数据指标看板发布上线。
-

预计本周五会完成5.0分支虚拟设备(Sim/Qemu)CT/Daily任务配置，下周二任务能稳定运行。下周五完成硬件设备(x4b/O62)任务配置，并在3.26号硬件设备任务稳定运行。
```

``` 在一个标准的rst 文档中，包含了多个md文件， 现在需要将所有的md 文档的内容原样替换md位置的内容。在rst文档中的.. doxygenfile:: media_policy.h，需要替换相同路径的media_policy.md中的内容

遍历api 目录，在每一级目录中，包含一个或多个目录，或者一个或多个markdown文档。将api 目录的子目录和子文档输出为一个json格式，在每个json item中包含一个parentId 记录了父级目录的id。如果是目录，则目录名称则为对应名称，如果是markdown文档，则第一行则为文档的名称
---
对 `api` 目录进行遍历操作，该目录下的每一级子目录中，都包含着一个或多个子目录，或者一个或多个 Markdown 文档。
请将 `api` 目录及其所有层级的子目录和子 Markdown 文档的相关信息，以 JSON 格式输出。在生成的 JSON 数据中，每个条目（item）都需包含一个 id、name、parentId 字段，parentId用于记录当前条目对应的父级目录的 ID。
若条目对应的是目录，则以该目录的名称作为相关名称信息；若条目对应的是 Markdown 文档，则以该文档的第一行内容作为文档的名称信息。
```
按照json 生成一个新的目录，
```
1. 将monkey自动化 的排期
2. 将资源规划 也纳入
3. 自动提单，自动分析
4. 
```

1. 先遍历整个markdown 文档，将所有的代码块都存储到一个map中，使用一个唯一md5 随机值做占位，等生成完成html之后，再将map中的 代码块解析生成html 并且替换原样位置。

```
在处理 Markdown 文档并将其转换为 HTML 的过程中，采用以下步骤优化处理流程：首先，遍历整个 Markdown 文档，将文档内所有代码块提取出来，并存储到一个映射（map）结构中。为每个代码块生成一个唯一的 MD5 随机值，以此作为该代码块在原 Markdown 文档中的占位符。当 Markdown 文档整体转换为 HTML 后，依据存储在映射中的代码块，将其解析并生成对应的 HTML 代码，最后利用之前生成的 MD5 随机值，精准地将这些生成的 HTML 代码替换回原 Markdown 文档转换为 HTML 后的相应位置，从而确保代码块在 HTML 中的正确呈现。
```

```
周报
百度 sim/o62lte/o62bt sdk导出
2. rel-5.0 分支开启 CT/Daily 任务， 目前基本已经具备开启条件
3. 开源文档网站，解决 40+ 文档转换问题 https://xiaomi.f.mioffice.cn/docx/doxk4J2jaRJLCrqRkwZVsr3e3ah
4. 自动化测试系统TMG 材料准备：https://xiaomi.f.mioffice.cn/wiki/YlXqwJTISi5CMtkgRAakQnZT4Tf
5. 
```

2. 新增功能、已有功能可能有性能回退，需要通过工具看护**防止回退，分析工作量大**
3. - **性能分析****工具**: 完善性能分析工具，通过日常看护发现性能倒退点（见快应用框架自动化测试）

## 专利篇
1. 一种嵌入式系统的CICD流程优化方法


研发基于Feature开发中，CT测试、Daily测试、性能测试和Monkey测试都需要使用测试资源，构建一套科学、高效的设备管理流程，可大幅提升整体测试效率。

```bash
测试用例开发、qemu稳定性测试和硬件设备稳定性测试、测试用例耗时、TSE审批、研发负责人审批、研发合入
```

```bash
资源池包含3类测试资源
利用虚拟化技术来优化嵌入式系统测试的方案
面向车用嵌入式系统的虚拟化测试平台及其实时性验证方法
基于虚拟化资源池化的嵌入式操作系统测试效率优化方法


测试框架使用效率
- 测试环境自动化配置
- 完备的设备管理池
- 测试用例合入班车流程化
提升系统测试质量
- 实现Qemu/Sim Monkey 自动化
- 实现CT测试任务并发执行
- 系统性能监控与预警
- 用例稳定性测试工具
特性测试 的所有测试也在看板可看到

# 相同组的测试产品的结果合并到一起
alter table vela_coralsea  
    add group_name varchar(64) null comment '分组名称';
```

```
三元组和绑定配置文件存储和访问接口

三元组作为一个公共资源

我的理解一个三元组信息就是一个 压缩包，压缩包按照标准的目录结构存储， 解压之后就可以直接使用里面的文件。
三元组信息需要包含 
三元组的hash值、三元组的名称、三元组的状态、三元组存储的url、当前使用三元组的任务编号，使用开始时间，分组名称
三元组的使用记录，当前是哪个任务在使用三元组，
具体分组根据上传的task_key 作区分
需要一个表记录三元组的分组信息，就是这些三元组信息可用于哪些产品
```

```sql
CREATE TABLE triplets (
	id int(8)  PRIMARY KEY,
    triplet_hash VARCHAR(255) COMMENT '三元组哈希值（唯一标识）',
    status ENUM('available', 'in_use', 'maintenance') NOT NULL DEFAULT 'available' COMMENT '状态',
    storage_url VARCHAR(512) NOT NULL COMMENT '存储URL',
    current_task_id VARCHAR(255) DEFAULT NULL COMMENT '当前使用该三元组的任务编号',
    usage_start_time DATETIME DEFAULT NULL COMMENT '当前使用开始时间',
    group_name VARCHAR(255) NOT NULL COMMENT '所属分组',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
);
```
1. 新增三元组信息
新增三元组上传的是一个csv格式的超链接，先下载该csv文件到本地，解析该csv文件，获取 storage_url、group_name、status 字段并批量导入到数据库中
2. 获取一个空闲的三元组信息
3. 任务结果接收消息队列传递过来的消息，同时也会轮询去查，看当前三元组是否有超时未释放的，需要做预警通知处理

测试任务状态监控与预警
自动化测试执行过程的监控与预警是保障测试稳定性的重要功能。
需要做一个各分支各产品自动化测试状态的看板，如果状态不正常的话需要主动报警
将以分支列出所有的自动化测试产品测试任务的状态，每个任务有3中状态，未执行、执行中和执行失败。任务显示的是最新的任务状态
如果是daily任务查询的是当天9点之前触发的任务，如果是性能测试
性能测试 关注每天的任务的执行结果
monkey测试则关注最近一次任务的执行状态
我们需要通过看板能直观展示不同分支重点关注任务的测试状态，同时，测试运营人员根据任务结果的准确性， 将报警信息发送到群里。

``` 解析csv文件内容， 分别获取elf_link、dump_log_path 两列的值， 执行wget将这两个值下载到crashlog 目录中，
将cmd中dump_log_path替换为elf_link 的文件名，将cmd中elf_path替换为 elf_link文件名。
执行cmd 命令获取命令输出的结果。
```

周报：
```
1. 自动化测试系统 TMG材料准备
2. Qemu/Sim Monkey自动化实现， 三元组资源池功能研发
3. 基于crash分析，自动化提报jira 工具
```

```sql
CREATE TABLE `test_triplets` (  
  `id` int(8) unsigned NOT NULL AUTO_INCREMENT,  
  `did` varchar(32) DEFAULT NULL COMMENT 'did',
  `key` varchar(32) DEFAULT NULL COMMENT 'key',  
  `mac` varchar(32) DEFAULT NULL COMMENT 'mac',   
  `triplet_hash` varchar(32) DEFAULT NULL COMMENT '三元组哈希值',    
  `status` varchar(20) NOT NULL DEFAULT 'available' COMMENT '状态',  
  `storage_url` varchar(512) NOT NULL COMMENT '存储URL',  
  `current_task_id` varchar(255) DEFAULT NULL COMMENT '当前使用该三元组的任务编号',  
  `usage_start_time` datetime DEFAULT NULL COMMENT '当前使用开始时间',  
  `group_name` varchar(255) NOT NULL COMMENT '所属分组',  
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',  
  PRIMARY KEY (`id`)  
) ENGINE=InnoDB AUTO_INCREMENT=4 DEFAULT CHARSET=utf8mb4

alter table vela_crash_resource  
    add create_jira_status int(8) null comment '是否创建JIRA';
```

自动分析文件，自动指派jira
自动化测试框架

嵌入式系统自动化测试框架，高效测试嵌入式功能

新建一个数据表
id、branch、product task_name、team、domain、 update_time
```
CREATE TABLE `task_org_mapping` (
  `id` int(10) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `branch` varchar(64) DEFAULT NULL COMMENT '分支',
  `product` varchar(32) DEFAULT NULL COMMENT '产品',
  `task_name` varchar(128) NOT NULL COMMENT '任务名称',
  `team` varchar(32) DEFAULT NULL COMMENT '团队',
  `domain` varchar(32) DEFAULT NULL COMMENT '领域',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
   PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='任务组织映射表';

CREATE TABLE `vela_crash_backtrace_history` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `crash_thread_name` VARCHAR(255) NOT NULL,
    `normalized_backtrace` TEXT NOT NULL,
    `original_backtrace` TEXT NOT NULL,
    `jira_issue_key` VARCHAR(50) DEFAULT NULL,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `team` VARCHAR(100) DEFAULT NULL,
    `domain` VARCHAR(100) DEFAULT NULL,
    `resource_id` VARCHAR(50) NOT NULL,
    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

```

```
1. 查询本次上传的全部解析任务，提取 diag_report 数据。
2. 从 diag_report 中获取 crash thread 以及 backtrace 信息。
3. 依据 crash thread 进行排序，选取排序后的第一个。
4. 检索映射关系表，定位与之对应的团队。
5. 为团队负责人创建 JIRA，将所有发生的 crash、backtrace 信息以及 crash 分析报告链接，一并添加至 JIRA 中。
```

```
1. 查询所有节点信息，使用多进程同时并发更新仓库
```

```
周报
1. 自动测试系统 TMG评审通过，里程碑任务按优先级拆分,明确8个子目标达成时间里程碑
2. Qemu-miwear Monkey自动化全链路联调基本完成，
3. 自动化测试系统中3个专利点挖掘
```

1,节点删除了重新创建新的 zhangchao
2.5个问题 