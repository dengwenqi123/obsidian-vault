1. 需要区分自研 和三方 整个
2. 需要区分不同分析 dev-system 和 trunk-26rc01

需要做的事情
1. 将映射关系导入到数据库中
2. 定期同步和汇总这些数据到汇总数据中


测试的目标：
1. 可以创建成功 rwt
2. AI 调用python 工具，能创建 git-worktree, 可以作为agent 的一个tools，这样可以保证，
3. AI 在修改代码，提交git-commit也可以使用一个python 程序，封装成一个tools

我只需要修改，增加tools 部分， tools 增加两个 一个是 amend 提交， 一个是 push gerrit;
