1. 将bug 全部同步到线上。


问题清单
1. 没有基于日志提前backtrace 进行提单
2. 还是存在混着提单的jira
	1. 提单中没有 对应产品的标签 p62 
3. 没有自动pin 到jira 的顶部
4. 代码仓库需要更新
5. 主线的代码需要合入
6. 需要将coverity 问题导入
7. 开发完成的提单流程，完整验收
8. 需要在主线上开通 coverity 问题的扫描
9. 需要调整和规划节点，每一个节点可以同时分析多种问题
10.  需要在平台增加入口访问，
11. 其中论坛是没有session 的概念的，不能与他交互执行； 每一次都从头来
12. 聊天框是有session 的概念，可以一直聊天，继续追问。 继续分析，类似本地的claude 启动一个会话  1
13. 汽车coverity 的名单需要重新刷一下
14.  直接在issue 上有一个入口，点击可以直接将该问题带入到聊天框，创建一个新的会话进行聊天。 1

15. 状态没有同步， 还有一些基本的jira 没有同步过来  1 
16. 在右侧有一个固定的位置显示  1
17. 直接将 AI 分析的报告链接补充到jira 的desription中 1
18.  需要在ai 分析完成后，调用接口自动将  1

19. 在 小助手创建完jira 后，需要自动将该jira 的信息同步到 multica 平台
20. 大家可以将自己本地的节点注册上来为私有的，解决自己的问题

将所有的jira, 包括功能性和稳定新的jira bug同步过来， 
可以基于comments中的crash 问题场景一个issue 进行分析。

可以让AI 自动上一个patch 上来
需要指派给对应的研发

1. 多agent 注册到 velaAgent 平台，并且测试这部分的准确性和稳定性。
2. 调研多agent 的组队功能，建立通用的解Bug agent, 底部切分为不同的多个agent，通过路由到不同的agent 进行干活
3. 需要补充一个 总结与反馈的agent，这个agent 可以总结该问题，并且输出一个卡片，让研发进行确认，确认完成后，将数据沉淀到知识库中
4. 完成 普通Bug 和 稳定性Bug 的测试，主要是P62, 还有已经支持哪些 P62 的功能性问题
5. 支持配置jira filter， 按照jira filter将jira 已有的数据实时同步到velaAgent 平台
6. 支持小象的关联，优先级低。
7. 开放 研发将本地runtime 和 agent 功能开发，让大家将自己本地的claude 注册到云端
反馈率

 创建问题出错了
 需要配置团队，做好agent 路由和编排
 需要在每一个问题出，增加运行的详细信息
 直接提供接口，通过 issue_key 进行查询即可，无需直接存储该链接
 数据同步，支持需要设置通用的同步规则
 线上需要处理 shenliping 不能登陆的问题。
 
# 问题列表
1. resolve 的单子， 主单已经close, 也是需要close  1
2. 确认是否真的可以pin 到顶部 1
3. reopen 单子没有指派给正确的人 1
4. 日志 自动分析
5. p62 和 p67 的单子现在还是混到一起 数据需要清理 1
明天上午统一跑一下 历史的时间


初始化设置命令需要改
需要大家一个本地的开发环境，用于共同协作开发


 需要在页面http://vela.agent.xiaomi.srv/vela/issues/bf76428f-6002-4cfb-8312-86e0903aec92 
  输入框部分增加一个按钮，点击按钮可以调用一个通用agent,对这个问题解题过程进行总结，总结完成后，有一个按钮，用户确认后，将这条经验存储到数据库中，状态修改为已确认 /brainstorming /karpathy-guidelines

需要开启 p2 的 coverity 扫描的问题 1
现在ai的分支，直接回复的 AI 分析报告链接需要为velaagent 平台。
将全闭环流程也接入进去。
细化的数据指标，数值指标的运营
需要每天将这些未处理的patch， 增加一个汇总的飞书通知，通知群 【Vela内部】MANN ZCU/DCU/ADD/XCD项目群 每天下午4点通知

我们在 当前 p62 项目 的 patch 的check 打分里增加一项，检测如果 jira 是 crash 问题， 并且没有 反馈 ai 分析信息是否有效 ，就会打分-1 ，block 合入

将msg 回写
需要ok
现在dev-system patch 比较少，原因 哪里漏了需要提上来
需要增加飞书通知
两者二合一 包括本地的和云端的
数据任务状态同步

请在 vela05 -vela07 启动 cron， 每一个1小时更新代码至最新
将机器都放到统一的源码位置：
需要考虑将普通的jira 也同步过去。

状态同步不准确
不同账号的测试

所有的jira 都需要同步过去。需要在分析的jira 中自动追加分析的链接。
现将 jira.n 和 miwear 未close 最近2个月的bug 导入到这个平台。
需要在vela-tools 做一个同步模块，在模块中能设置同步的jql, 以及同步了多少数据，状态，最近什么时候同步的，有没有同步失败的。 支持手动同步，默认是自动同步的。

指标一定要细化
-  ai 反馈了多少，多少有效，多少无效。
如果当前tools 中不包含 pynuttx， 则认为是无效的 tools, 不上传。

crash 提报的jira 和commits 需要提供的是 AI分析平台的链接，点击链接进入

需要给系统的各项功能都增加数据打点，数据打点上报到oneTrack 平台，需要实现一个独立的数据打点的模块，